package objectbooks;

public class Strings {
	//
	// /**
	// * @param args
	// *
	// *
	// * /*
	// * "<Book>" is a generic type. It tells the constructor what is
	// * in the ArrayList. We generics to save ourselves
	// * the trouble type casting
	// * "new ArrayList <?>()" constructor does not need to specify
	// * length.(Default is 10, but it doesn't matter,
	// * it adjusts as you add objects to it)
	// * note that ArrayList is Indexed (item 0 is always at index 0
	// * item 1 is always at index 1 and so on). You can iterate through it
	// * you cannot make an ArrayList of primitives (no int,boolean,etc.)
	// *
	// *If you wish to make an ArrayLists of ints, you use the wrapper class
	// stanley is gay
	// *int is Integer, double is Double, etc.
	// *
	// */
	// // add things to an ArrayList (remember , for arrays, we did
	// //array[0] = book1; // specify index
	// shelf.add(book1);
	// //doesn't specify index, book1 is auto index0
	// shelf.add(book2);
	// shelf.add(book3);
	// //you can construct at the same time;
	// shelf.add(new Book("The Man",20,author3));
	// //to get something from an ArrayList(recall to get something)
	// //from array1
	// //System.out.println(array[0]);
	// //System.out.println("The first book on the shelf is " + shelf.get(0));
	// //Loses points if you do this wrong
	//
	// //iterate through an ArrayList;
	// //for(Book b:shelf)
	// // System.out.println(b);
	//
	// //to get the length of ArrayList
	// //recall using arrays;
	// //array.length
	// //System.out.println("The length(size) of the shelf is "+shelf.size()+" books");
	//
	//
	// Book book5 = new Book("The Minority Report", 509,author2);
	//
	// //puts it first on the shelf
	// shelf.add(0,book5);
	//
	//
	// //it removes stuff
	// //shelf.remove(book1);
	// //shelf.remove(0);
	// for(int i=0;i<shelf.size();i++)
	// while(i<shelf.size()&&shelf.get(i).getTitle().toLowerCase().indexOf(the.toLowerCase())>=0)
	// shelf.remove(i);
	// for(int z=0;z<shelf.size();z++)
	// System.out.println(shelf.get(z));
	//
	// if(shelf.contains(book2)){
	// System.out.println(book2.getTitle() + "is book#"+shelf.indexOf(book2)+
	// "on the shelf.");
	// }
	//
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
